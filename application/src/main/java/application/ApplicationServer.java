package application;

public class ApplicationServer {

	/*
	 * Thread crm = new Thread(new CrmServer(null)); Thread crm = new
	 * Thread((Runnable) new CrmSpringApplication(args)); Thread billing = new
	 * Thread(new BillingServer());
	 * 
	 * billing.start(); crm.start();
	 */

}
